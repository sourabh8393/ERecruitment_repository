<%= form_for(@relation_question) do |f| %>
  <% if @relation_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relation_question.errors.count, "error") %> prohibited this relation_question from being saved:</h2>

      <ul>
      <% @relation_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :my_question_id %><br>
    <%= f.number_field :my_question_id %>
  </div>
  <div class="field">
    <%= f.label :ttest_question_id %><br>
    <%= f.number_field :ttest_question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
